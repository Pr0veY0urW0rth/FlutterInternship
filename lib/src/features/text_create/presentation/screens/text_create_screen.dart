import 'package:flutter/material.dart';

class TextCreateScreen extends StatelessWidget {
  const TextCreateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
